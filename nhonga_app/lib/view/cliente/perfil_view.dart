import 'package:flutter/material.dart';



class Perfil_view extends StatelessWidget {
  const Perfil_view({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Perfil'),
    );
  }
}