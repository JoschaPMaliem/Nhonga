import 'package:flutter/material.dart';

class Adicionar_Produto extends StatelessWidget {
  const Adicionar_Produto({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text("Adicionar produto"),
      ),
    );
  }
}
