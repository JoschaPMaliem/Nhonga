import 'package:flutter/material.dart';

class Editar_Produtos extends StatelessWidget {
  const Editar_Produtos({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(

      ),

      body: Center(
        child: Text("Editar produtos"),
      ),
    );
  }
}